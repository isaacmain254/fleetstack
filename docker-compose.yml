services:
  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - backend
  #   environment:
  #     NEXT_PUBLIC_API_URL: http://localhost:8080

#   backend:
#     build: ./backend
#     ports:
#       - "8080:8080"
#     depends_on:
#       - postgres
#       - redis
#     environment:
#       DATABASE_URL: postgresql://postgres:postgres@postgres:5432/platform
#       REDIS_URL: redis://redis:6379

#   worker:
#     build: ./worker
#     privileged: true
#     volumes:
#       - /var/run/docker.sock:/var/run/docker.sock
#       - ./data:/data
#     depends_on:
#       - backend
#     environment:
#       API_URL: http://backend:8080

  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: fleetstack_platform
      POSTGRES_USER: fleetstack_user
      POSTGRES_PASSWORD: fleetstack_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # redis:
  #   image: redis:8

#   traefik:
#     image: traefik:v3
#     command:
#       - --api.insecure=true
#       - --providers.docker=true
#       - --entrypoints.web.address=:80
#       - --entrypoints.websecure.address=:443
#     ports:
#       - "80:80"
#       - "443:443"
#       - "8081:8080"
#     volumes:
#       - /var/run/docker.sock:/var/run/docker.sock

volumes:
  postgres_data:
